{
  "name": "eslint-plugin-svelte-custom-element",
  "version": "2.0.0",
  "description": "Eslint plugin with rules for using svelte with custom elements as compile targetRules for using svelte with custom elements as compile target",
  "main": "index.js",
  "scripts": {
    "test": "mocha test --recursive",
    "tdd": "npm test -- --watch --reporter min",
    "prerelease": "npm test && git diff --quiet || exit 1",
    "release": "./scripts/release.sh",
    "postrelease": "git push --no-verify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@babel/types": "7.10.4",
    "change-case": "4.1.1"
  },
  "devDependencies": {
    "eslint": "7.4.0",
    "mocha": "8.0.1"
  }
}
